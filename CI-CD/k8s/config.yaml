apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    env: demo-cluster
    type: backend
spec:
  containers:
  - name: nginx-container
    image: nginx
    ports:
    - containerPort: 8080
       
