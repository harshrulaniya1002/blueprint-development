apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport-svc
  labels:
    env: demo
    svc: nodeport
spec:
  type: NodePort
  ports:
  - nodePort: 30001
    port: 80
    targetPort: 80
  selector:
     env: demo-pod
